// nuxt.config.js
export default defineNuxtConfig({
  // Specify any runtime config if needed
  runtimeConfig: {
    public: {
      // Publicly accessible config
    },
    private: {
      // Private config
    }
  },

  // Modules and plugins can be added here
  modules: [
    // Example module
  ],

  // Other settings like serverMiddleware or build configuration
  build: {
    // Customize the build process if needed
  }
});
